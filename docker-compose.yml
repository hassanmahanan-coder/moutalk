services:
  postgres:
    image: postgres:16-alpine
    container_name: moutalk-postgres
    environment:
      POSTGRES_USER: moutalk
      POSTGRES_PASSWORD: moutalk_dev_pw
      POSTGRES_DB: moutalk
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U moutalk"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: moutalk-redis
    ports:
      - "6380:6379"
    volumes:
      - redisdata:/data

  celery_worker:
    build:
      context: ./backend
    container_name: moutalk-celery-worker
    environment:
      # 开发：连宿主机 Redis（backend 在本机用 localhost:6379）。
      # 生产：改为 redis://redis:6379/1（容器内网络）。
      CELERY_BROKER_URL: redis://host.docker.internal:6379/1
      CELERY_RESULT_BACKEND: redis://host.docker.internal:6379/2
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      PDF_OUTPUT_DIR: /app/media/reports
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./backend:/app
    depends_on:
      - postgres
      - redis
    command: ["celery", "-A", "app.celery_app", "worker", "--loglevel=info", "--pool=solo"]

  celery_beat:
    build:
      context: ./backend
    container_name: moutalk-celery-beat
    environment:
      CELERY_BROKER_URL: redis://redis:6379/1
      CELERY_RESULT_BACKEND: redis://redis:6379/2
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      PDF_OUTPUT_DIR: /app/media/reports
    volumes:
      - ./backend:/app
    depends_on:
      - postgres
      - redis
    command: ["celery", "-A", "app.celery_app", "beat", "--loglevel=info"]

  backend:
    build:
      context: ./backend
    container_name: moutalk-backend
    environment:
      CELERY_BROKER_URL: redis://redis:6379/1
      CELERY_RESULT_BACKEND: redis://redis:6379/2
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      PDF_OUTPUT_DIR: /app/media/reports
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    depends_on:
      - postgres
      - redis
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]

  # Milvus 完整版（生产，PRD 9.2 选项 B）：standalone + etcd + minio。
  # 对齐官方 v3.0.0 standalone compose（milvus-io/milvus v3.0.0）；
  # pymilvus 3.0.1 ↔ Milvus 3.0.x（官方兼容表）。3.0 默认 Woodpecker 嵌入式消息队列。
  milvus-etcd:
    image: quay.io/coreos/etcd:v3.5.25
    container_name: milvus-etcd
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 20s
      retries: 3
    volumes:
      - etcddata:/etcd

  milvus-minio:
    image: minio/minio:RELEASE.2024-05-28T17-19-04Z
    container_name: milvus-minio
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    ports:
      - "9001:9001"
      - "9000:9000"
    volumes:
      - miniodata:/minio_data
    command: minio server /minio_data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  milvus-standalone:
    image: milvusdb/milvus:v3.0.0
    container_name: milvus-standalone
    command: ["milvus", "run", "standalone"]
    security_opt:
      - seccomp:unconfined
    environment:
      MINIO_REGION: us-east-1
      ETCD_ENDPOINTS: milvus-etcd:2379
      MINIO_ADDRESS: milvus-minio:9000
    ports:
      - "19530:19530"
      - "9091:9091"
    volumes:
      - milvusdata:/var/lib/milvus
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9091/healthz"]
      interval: 30s
      start_period: 90s
      timeout: 20s
      retries: 3
    depends_on:
      - milvus-etcd
      - milvus-minio

volumes:
  pgdata:
  redisdata:
  milvusdata:
  etcddata:
  miniodata:
